<form action="/delete/{{id}}" method="post">
  <span>Are you sure you want to delete <strong>{{title}}</strong>?</span><br />
  <input type="hidden" name="_csrf" value="{{csrfToken}}" />
  <input type="submit" name="submit" value="Delete" />
</form>
