<article>
  {{#if draft}}
    <h3>[DRAFT] {{title}}</h3>
  {{else}}
    <h3>{{title}}</h3>
  {{/if}}
  <p>{{{content}}}</p>
  <div>Posted in: {{category}}</div>
  Tags:
  <ul>
    {{#each tags}}
      <li>{{.}}</li>
    {{/each}}
  </ul>
</article>
